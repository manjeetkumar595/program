#include<iostream>
using namespace std;
int main()
{
    int roll;
    cout<<"Enter roll no. :";
    cin>>roll;
    if(roll>0)
     cout<<"Valid roll no.";
    else
     cout<<"Invalid roll no.";
    return 0;
}